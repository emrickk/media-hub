# Copy to sync-config.env, fill in, then: chmod 600 sync-config.env
# Gitignored. Read via secrets.py — never hardcode these anywhere.
# Values containing '=' (base32 keys, JWTs) are fine; only the first '=' splits.

plex_url=
plex_token=

ryot_url=
ryot_api_key=
ryot_admin_token=
ryot_password=

yamtrack_url=
yamtrack_password=

tmdb_api_key=
tmdb_read_token=
